/**
 * @paybot/sdk — TypeScript SDK for PayBot
 *
 * Provides bot developers with a simple interface to integrate
 * PayBot payment infrastructure into their AI agents.
 */

export { PayBotClient } from './client.js';
export type {
  PayBotConfig,
  PaymentRequest,
  PaymentResponse,
  PaymentStatus,
} from './types.js';
