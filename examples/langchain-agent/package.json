{
  "name": "paybot-example-langchain-agent",
  "version": "1.0.0",
  "description": "Agent-pays-for-API pattern: a minimal agent-tool loop that pays a 402 via createX402Handler.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node --import tsx index.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "paybot-sdk": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.3.0"
  }
}
