{
  "name": "paybot-example-multi-bot-pool",
  "version": "1.0.0",
  "description": "PayBotClientPool with a shared treasury, payAs(), and the TREASURY_EXCEEDED guard.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node --import tsx index.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "paybot-sdk": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.3.0"
  }
}
