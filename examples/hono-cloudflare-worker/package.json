{
  "name": "paybot-example-hono-cloudflare-worker",
  "version": "1.0.0",
  "description": "Hono Cloudflare Worker that protects a route with a manual x402 payment wall.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.78.0"
  }
}
